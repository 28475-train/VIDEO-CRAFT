function toggleRuby() { document.body.classList.toggle('show-ruby'); }function googleTranslateElementInit() {new google.translate.TranslateElement({pageLanguage: 'ja', layout: google.translate.TranslateElement.InlineLayout.SIMPLE}, 'google_translate_element');}function sendLineOrder() {const type = document.getElementById('v-type').value;const date = document.getElementById('v-date').value;const publicStatus = document.getElementById('v-public').value;const memo = document.getElementById('v-memo').value;const message = `【映像制作の申し込み・相談】
動画の種類：${type}希望納期：${date}実績公開：${publicStatus}備考・要望：${memo}上記の内容で相談したいです。`;const encodedMessage = encodeURIComponent(message);
const lineUrl = `https://line.me/R/oaMessage/@494zixmx/?${encodedMessage}`;

window.location.href = lineUrl;
}function renderNav() {const top = document.getElementById('top-nav');const bottom = document.getElementById('bottom-nav');const path = window.location.pathname.split("/").pop() || 'index.html';const menu = [{n:'About', u:'about.html'}, {n:'Works', u:'works.html'}, {n:'Price', u:'price.html'},{n:'Flow', u:'flow.html'}, {n:'Guide', u:'guide.html'}, {n:'Contact', u:'contact.html'}];const html = menu.map(m => <a href="${m.u}" class="nav-item ${path===m.u?'active':''}">${m.n}</a>).join('');if(top) top.innerHTML = `<div class="nav-inner"><div class="nav-row-1"><a href="index.html" class="logo-link">KIZUNA VIDEO</a><button onclick="toggleRuby()" class="ruby-toggle">ふりがな</button></div><div class="nav-row-2">${html}</div></div>`;
if(bottom) bottom.innerHTML = `<div class="nav-inner" style="padding:10px;"><div class="nav-row-2" style="justify-content:center;">${html}</div></div>`;
}document.addEventListener('DOMContentLoaded', renderNav);
